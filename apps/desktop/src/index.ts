// SPDX-License-Identifier: Apache-2.0
// This app is a thin Tauri v2 wrapper — it contains no feature logic.
// See ../web for the actual application; the real src-tauri/ setup (which
// needs a Rust toolchain) lands in a later Milestone 0 step.
export {};
