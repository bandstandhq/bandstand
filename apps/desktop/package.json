{
  "name": "@bandstand/desktop",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node -e \"if (!require('node:fs').existsSync('../web/dist')) { console.error('apps/web must be built first'); process.exit(1); }\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  }
}
